# 任意インストール: ページ OCR(セリフ抽出)機能
#   ./.venv/Scripts/python.exe -m pip install -r requirements-ocr.txt
# mokuro = comic-text-detector(テキスト領域検出) + manga-ocr(日本語漫画 OCR)。
# torch を含むため数百 MB〜数 GB 級。未導入でも OCR 以外の機能は全て動く。
#
# GPU で動かす場合は、PyPI の torch が CPU ビルドのため CUDA 版に入れ替える
# (Blackwell 世代は cu128 以降が必要):
#   ./.venv/Scripts/python.exe -m pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
# CUDA が使えれば GPU、無ければ CPU で自動選択される(app/ocr.py)。
mokuro>=0.2.5,<0.3
